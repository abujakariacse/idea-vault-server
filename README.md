# IdeaVault Server

Backend API for the IdeaVault platform.

## Setup

1. Copy `.env.example` to `.env`
2. Add MongoDB URI, JWT Secret, and Google Client ID
3. Run `npm install`
4. Run `npm start`

## Tech Stack

- Express.js
- MongoDB + Mongoose
- JWT Authentication
- Google OAuth
- bcryptjs
